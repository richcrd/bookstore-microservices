# Changelog

Todos los cambios notables de **bookstore-microservices** se documentan en este archivo.

El formato sigue [Keep a Changelog](https://keepachangelog.com/es/1.1.0/) y el proyecto se adhiere a [Semantic Versioning](https://semver.org/lang/es/). Cada entrada relaciona su tipo con los [Conventional Commits](CONTRIBUTING.md#conventional-commits) y referencias de código/PR.

## [Unreleased]

### Added

- **Proveedor OpenID Connect / OAuth 2.0** (OpenIddict 7) en Auth.API: página de login en `/connect/authorize`, token endpoint `/connect/token` con *password*/*refresh* (cliente `cli`) y *Authorization Code + PKCE* (cliente `web-spa`), discovery en `/.well-known/*`.
- **Validación por discovery (RS256)** en Catalog/Orders/Inventory vía `OpenIddict.Validation` (SharedKernel `AddIdpAuthentication`): los servicios descargan claves y validan `iss` sin firma compartida hardcodeada.
- **API Gateway YARP** como punto único de entrada (`:5080` dev, `:80` prod) con rutas `/api/v1/*` hacia catalog, orders e inventory y endpoints OIDC (`/connect/*`, `/.well-known/*`) hacia auth.
- **Orden Saga orquestada** con `MassTransit`: state machine (pendiente → pago → reserva → `Shipped`), con **outbox/inbox transaccional** (sin pérdida ni duplicados).
- Consumidores RabbitMQ: `OrderCreatedConsumer` y `OrderStatusChangedConsumer` en Inventory; endpoint `change-order-status` en Orders.
- **Idempotencia** en la creación de pedidos vía header `Idempotency-Key` (GUID): `201` la primera vez, `200` con el mismo pedido en reintentos; clave generada automáticamente si no se envía.
- **Migraciones EF automáticas** al arrancar en Catalog, Orders, Inventory y OrderSaga.
- Script `docker/postgres/init/001-create-databases.sql` que crea `catalog_db`, `orders_db`, `inventory_db` y `order_saga_db` en entornos limpios.
- **Observabilidad**: OpenTelemetry → Jaeger (OTLP), Prometheus + Grafana provisionado, y `docker-compose.observability.yml`.
- **Docker de producción** multi-stage (`docker/docker-compose.prod.yml`, 9 contenedores, gateway en `:80`).
- **CI/CD** en GitHub Actions: `ci.yml` (build + test en cada push) y `cd.yml` (deploy por tags `v*`).
- Suite de **94 tests** (unit + integración con Testcontainers; ver `tests/ApiGateway.UnitTests`).
- **Frontend SPA** (fuera del monorepo, `~/Desktop/BookStoreWeb`): React 19 + Vite 8 + TypeScript 6, react-router 7, TanStack Query 5, axios, zod 4 y oidc-client-ts 3; consume el backend por el gateway (`:5080`) con Authorization Code + PKCE (cliente `web-spa`), login HTML en `/connect/authorize`, callback en `/callback`, renovación silenciosa (`automaticSilentRenew`) y logout con `signoutRedirect`.
- **Auth.API**: endpoint de logout `GET/POST /connect/logout` (`AuthorizationController.Logout()`): cierra la sesión de OpenIddict (`SignOutAsync`) y redirige a `post_logout_redirect_uri` (registrado `http://localhost:5173/` para el SPA) o `/`.
- **ApiGateway**: política CORS `Frontend` con `AllowedOrigins=["http://localhost:5173"]` y transform `RequestHeaderOriginalHost: true` en las rutas OIDC (`/connect/{**catch-all}`, `/.well-known/{**catch-all}`) para preservar el Host público `:5080` hacia Auth.
- **Configuración por entorno del SPA** (`.env`): vars `VITE_GATEWAY_URL` y `VITE_OIDC_CLIENT_ID` en `.env` (dev), `.env.production` (prod) y `.env.example` (plantilla versionable), con tipos en `src/vite-env.d.ts`. Son **configuración, no secretos**: se embuten en el bundle en *build time* y la seguridad del flujo SPA sigue siendo Authorization Code + PKCE con el cliente público `web-spa`.
- **Carrito persistente del SPA** (localStorage, clave `bookstore.cart`): dominio `Cart`/`CartItem` + puerto `CartStoragePort`, contexto `CartProvider`/`useCart()` con persistencia automática, página `/cart` con +/−, quitar, vaciar, aviso de login y checkout con `Idempotency-Key` que vacía el carrito al confirmar; `/orders/new` redirige a `/cart` y la nav muestra el contador «Carrito (n)».
- **Búsqueda en el catálogo del SPA** con debounce (`useDeferredValue`) y reseteo de página al cambiar el término; el `queryKey` de TanStack Query combina `search + page`.
- **Paginación de la UI del SPA**: componente reutilizable `Pagination` (Anterior · Página X de Y · Siguiente) en libros y pedidos, con `keepPreviousData` y `page` en el `queryKey`.
- **Convención de código del SPA**: código directo sin escritura defensiva ni helpers de más (`parse`/`normalize`/`safe`/`fallback`/`resolve` prohibidos como nombres propios; el `.parse()` de zod es API de librería).
- **ApiGateway: validación OIDC en el borde** (`feat(gateway)`): `AddIdpAuthentication` (OpenIddict.Validation de SharedKernel) + política `authenticated` (`RequireAuthenticatedUser`) en las rutas `orders` (`/api/v1/orders/{**catch-all}`) e `inventory` (`/api/v1/stock-items/{**catch-all}`) → **401** sin token; `catalog-books`, `catalog-categories`, `auth-connect` y `auth-wellknown` siguen públicas (semántica de cada servicio). Issuer dev del gateway `OpenIddict__Issuer: http://localhost:5080` (proxya su propio `/.well-known` a Auth; *bootstrap* sin bucle) y orden de middleware `UseServiceTelemetry → UseRateLimiter → UseCors → UseAuthentication → UseAuthorization → MapReverseProxy`.
- **ApiGateway: rate limiter extraído a `RateLimitPolicies.CreateGlobalLimiter()`** (fixed window **20 req/15 s por IP**, `QueueLimit 0`, **429** con `Retry-After: 15`) y cubierto por el nuevo **`tests/ApiGateway.UnitTests`** (2 tests: límite superado y partición por IP); la suite pasa de **92 a 94 tests**.
- **Frontend SPA v3 — seguimiento del pedido en vivo** (`feat(frontend)`, fuera del monorepo; el backend **no cambió**: el `OrderDto` de `GET /api/v1/orders/{id}` ya exponía `status`/`updatedAt`): nueva página `/orders/:id` (`OrderDetailPage`) con `OrderTracking` (timeline `Pending → Paid → Shipped → Delivered` resaltando la etapa actual, o aviso rojo si `Cancelled`), indicador pulsante «actualización en vivo», total/fechas/artículos y enlace «← Mis pedidos»; `useOrder(id)` y `useMyOrders` hacen *polling* (`refetchInterval` 2 s) mientras el pedido no esté en estado final (`isOrderFinished`: Shipped/Delivered/Cancelled — la saga termina en `Shipped`); nuevo `OrdersPort.getOrder(id)` (queryKey `orders.byId`) y módulo puro `src/application/orders/orderStatus.ts`.
- **Tests del frontend SPA** (`test(frontend)`, fuera del monorepo): kit **Vitest 5 + React Testing Library 16 + jsdom + jest-dom** (script `npm test` → `vitest run`; `vite.config.ts` con `test.environment: 'jsdom'` y `setupFiles: src/test/setup.ts` con cleanup tras cada test) y **12 tests** en 4 ficheros: `orderStatus.test.ts`, `CartStorageImpl.test.ts`, `CartContext.test.tsx` y `Pagination.test.tsx`. Los **94 tests** backend siguen en el monorepo; los 12 del frontend viven en el SPA.
- **Telemetría SharedKernel: export de logs por OTLP y soporte de `OTEL_EXPORTER_OTLP_ENDPOINT`** (`feat(sharedk)`): `AddServiceTelemetry` exporta ahora **trazas + métricas + logs** por OTLP **HTTP/protobuf** (protocolo explícito); el endpoint se resuelve por env `OTEL_EXPORTER_OTLP_ENDPOINT` (inyectada por Aspire) > config `OpenTelemetry:Endpoint` > default `http://localhost:4318` (collector). El worker `OrderSaga.Worker/Program.cs` replica el patrón inline, incluidos los logs OTLP (`WithLogging`) — Fase 19.
- **Stack observability v2** (`feat(docker)`): `docker-compose.observability.yml` con **otel-collector** (`otel/opentelemetry-collector-contrib:0.118.0`, OTLP gRPC `:4317` + HTTP `:4318`) que enruta **trazas → Jaeger** y **logs → Seq** (`datalust/seq`, UI `:5341`, volumen `seq-data`, auth deshabilitada); **Grafana** con datasource de uid fijo `prometheus`, provider de dashboards y **2 dashboards provisionados** (`BookStore · API`, `BookStore · RabbitMQ`) + **alertas** (latencia p95 > 1 s, ratio 5xx > 5 %, colas RabbitMQ > 200; contact point webhook placeholder); **Prometheus** con scrape de **RabbitMQ `:15692`** (`/metrics/per-object`); el contenedor dev `bookstore-rabbitmq` expone `15692` — Fase 19.
- **.NET Aspire AppHost** (`feat: add Aspire`): proyecto `src/AppHost/BookStore.AppHost.csproj` (SDK `Aspire.AppHost.Sdk/13.5.3`, `AspireUseCliBundle=true`) añadido a la solución; orquesta los 6 proyectos con sus puertos HTTP clásicos (`WithHttpEndpoint`) e inyecta `ConnectionStrings__CatalogDb/OrdersDb/InventoryDb/OrderSagaDb` y `RabbitMQ__Host` para la infra dev existente; dashboard en `https://localhost:17017` (inyecta `OTEL_EXPORTER_OTLP_ENDPOINT`); requisito `dotnet new install Aspire.ProjectTemplates`; alternativa a los 6 `dotnet run` — Fase 19.
- **Stack de observabilidad en el compose de producción** (`feat(docker)`): `docker-compose.prod.yml` incorpora **otel-collector** (`otel/opentelemetry-collector-contrib:0.118.0`, mount de `./otel-collector.yml`, puertos `4317`/`4318`), **Jaeger** (`jaegertracing/all-in-one:1.76.0`, `:16686`), **Seq** (`datalust/seq`, `:5341`, `ACCEPT_EULA`, `SEQ_FIRSTRUN_NOAUTHENTICATION`, volumen `seq-data`), **Prometheus** (`prom/prometheus:v2.55.0`, mount de `./prometheus.prod.yml`, volumen `prometheus-data`, retención 15 d, `:9090`, `--config.file` explícito) y **Grafana** (`grafana/grafana:11.4.0`, provisioning montado, volumen `grafana-data`, `:3000`, `GF_SECURITY_ADMIN_PASSWORD` por env con default `admin`) — Fase 20.
- **`docker/prometheus.prod.yml`** (`feat(docker)`): scrape de producción por **nombre de contenedor** del stack — job `bookstore` (`apigateway:5080`, `auth:5100`, `catalog:5038`, `orders:5248`, `inventory:5208`) + job `rabbitmq` en `rabbitmq:15692` con `metrics_path /metrics/per-object` — Fase 20.
- **Contact point de alertas de Grafana por entorno** (`feat(docker)`): `contact-points.yml` usa `${GRAFANA_ALERT_WEBHOOK_URL}` (interpolación de env de Grafana; el valor lo inyectan los composes con default `http://host.docker.internal:3001/hooks/none`) y `policies.yml` enruta al receiver `alert-webhook` — Fase 20.
- **CD propaga el webhook de alertas** (`feat(ci)`): `cd.yml` pasa `GRAFANA_ALERT_WEBHOOK_URL: ${{ secrets.GRAFANA_ALERT_WEBHOOK_URL }}` al servidor remoto (`env` + `envs:` del `appleboy/ssh-action`) para que el compose de prod interpole el contact point — Fase 20.
- **`appsettings.Development.json` por servicio** (`feat(docker)`/`feat(auth)`): nuevos archivos de desarrollo (×5: Auth, Catalog, Orders, Inventory, OrderSaga.Worker) con las credenciales de dev — connection strings `Password=postgres`, en Auth además usuarios demo `admin/admin123`/`customer/customer123` y `CliClientSecret: cli-dev-secret` con propiedad `"_comment"` explicativa — cargados **solo** con `ASPNETCORE_ENVIRONMENT=Development` — Fase 21.
- **Plantilla de secretos `docker/.env.prod.example`** (`feat(docker)`): variables obligatorias con fail-fast (`POSTGRES_PASSWORD`, `RABBITMQ_USER`, `RABBITMQ_PASSWORD`, `CLI_CLIENT_SECRET`, `GRAFANA_ADMIN_PASSWORD`) y opcionales (`POSTGRES_USER`, `BOOKSTORE_PUBLIC_ISSUER`, `GRAFANA_ALERT_WEBHOOK_URL`); `.env` gitignored — Fase 21.

### Changed

- Destinos del gateway configurables por entorno: `${ReverseProxy__Clusters__*__Destinations__*__Address}` para que los contenedores del stack de prod enruten por nombre de servicio y no por `localhost`.
- Los servicios con base de datos aplican `Migrate()` al arrancar en lugar de depender de dumps de BD.
- El token se obtiene ahora por OIDC a través del gateway (`POST /connect/token`, password grant) en lugar de `POST /api/v1/auth/token`.
- El issuer público del proveedor es configurable (`OpenIddict__Issuer`; en prod default interno `http://auth:5100`; `BOOKSTORE_PUBLIC_ISSUER` lo sobreescribe para dominios reales).
- Auth.API pasa a usar su propia base `auth_db` (aplicaciones/scopes/autorizaciones OpenIddict); el compose de prod la conecta y añade `depends_on: postgres`.
- El compose de prod inyecta ahora al servicio `apigateway` `OpenIddict__Issuer` (default interno `http://auth:5100`, sobreescribible con `BOOKSTORE_PUBLIC_ISSUER`) y `OpenIddict__DisableTransportSecurityRequirement: "true"`, igual que el resto de servicios con validación, para que el gateway valide tokens en el borde dentro de la red.
- **OpenTelemetry 1.12.0 → 1.18.0** (`feat(sharedk)`): bump de los paquetes OTel de `SharedKernel.csproj` (`OpenTelemetry.Extensions.Hosting`, `Instrumentation.AspNetCore`, `Instrumentation.Http` y `Exporter.OpenTelemetryProtocol` 1.18.0; `Instrumentation.EntityFrameworkCore` y `Exporter.Prometheus.AspNetCore` 1.18.0-beta.1) — corrige la vulnerabilidad **NU1902** (Fase 19).
- **OTLP en el stack de producción** (`feat(docker)`): todos los servicios del `docker-compose.prod.yml` usan ahora `OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318` (antes `http://host.docker.internal:4317`, telemetría inalcanzable en prod); en dev sigue `http://localhost:4318` — Fase 20.
- **Exportadores OTLP HTTP/protobuf con path por señal** (`feat(sharedk)`): `SharedKernel.Telemetry` y `OrderSaga.Worker/Program.cs` exportan a `{endpoint.TrimEnd('/')}/v1/traces`, `/v1/metrics` y `/v1/logs`; el endpoint base se configura **sin path** (dev `http://localhost:4318`, prod `http://otel-collector:4318`) y el SDK añade `/v1/{signal}` — Fase 20.
- **`appsettings.json` sin secretos** (`fix(security)` en ×5 servicios): eliminadas todas las credenciales en claro; quedan placeholders `CHANGE_ME` (fail-fast) en `ConnectionStrings.*` y en Auth (`OpenIddict__CliClientSecret: CHANGE_ME`, `AuthUsers.Users: []`) — los secretos se resuelven por entorno y **no viajan en el código ni en el compose** — Fase 21.
- **Compose de prod parametrizado con `${VAR}`** (`feat(docker)`): `docker-compose.prod.yml` interpola todos los secretos desde el entorno — `POSTGRES_PASSWORD` (sin default), connection strings con `Password=${POSTGRES_PASSWORD}`, RabbitMQ con `RABBITMQ_DEFAULT_USER/PASS`, orders/inventory/sagaworker con `RabbitMQ__Host` autenticado, auth con `OpenIddict__CliClientSecret: ${CLI_CLIENT_SECRET}` y Grafana con `GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}` (se elimina el default `-admin` de la Fase 20) — Fase 21.
- **CD inyecta los 6 secretos de entorno** (`feat(ci)`): `cd.yml` pasa `POSTGRES_PASSWORD`, `RABBITMQ_USER`, `RABBITMQ_PASSWORD`, `CLI_CLIENT_SECRET`, `GRAFANA_ADMIN_PASSWORD` y `GRAFANA_ALERT_WEBHOOK_URL` desde GitHub Secrets por `env` + `envs:` del `appleboy/ssh-action` (el compose los interpola en la sesión remota, sin `.env` en el servidor) — Fase 21.
- **Usuarios demo solo en desarrollo** (`fix(auth)`): en prod/qa ya **no** existen `admin/admin123` ni `customer/customer123` por defecto (movidos a `appsettings.Development.json`); opcionalmente se inyectan por env `AuthUsers__Users__N__*` — Fase 21.

### Fixed

- **CI**: la opción `cache: true` de `actions/setup-dotnet` fallaba en ausencia de `packages.lock.json`; se sustituyó por `actions/setup-dotnet@v6` sin caché y se subió `actions/checkout` a `v7`.
- Error de enrutado interno del gateway en contenedores (apuntaba a `http://localhost:5038` en vez de al nombre del servicio).
- El badge de estado del SPA pintaba los estados internos de la saga (`AwaitingPayment`/`PaymentApproved`/`ShipmentRequested`/`Completed`), que **nunca llegan a la API de orders** (por eso `Paid` se renderizaba en gris); ahora usa los estados reales de Orders (`Pending`/`Paid`/`Shipped`/`Delivered`/`Cancelled`) — Fase 18, fuera del monorepo.
- **Telemetría descartada en producción** (`fix(sharedk)`): los exportadores OTLP posteaban a la raíz del endpoint (`4318/`) y el receiver `otlp/http` del collector respondía **404**, con lo que trazas/métricas/logs se perdían en el stack de prod; ahora se exporta a `/v1/{signal}` y la telemetría llega a Jaeger, Seq y Prometheus — Fase 20.